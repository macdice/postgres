freebsd_instance:
  image_family: freebsd-12-1

task:
  install_script: pkg install gmake readline
  script: ./configure --enable-cassert --enable-debug --with-includes=/usr/local/include --with-libs=/usr/local/lib && gmake -s && gmake -s check
